let pao = false
let queijo = true

const breakFast = pao && queijo ? 'Café top' : 'Café ruim'
console.log(breakFast)

let age = 18
const idade = age >= 18 ? 'maior de idade' : 'menor de idade'
console.log(idade)